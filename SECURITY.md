# ReachSurge 安全说明

## 支持范围

ReachSurge 当前只支持本地 stdio MCP。请不要把进程端口、调试桥或未经认证的代理暴露到公网。项目尚未实现远程服务所需的认证、授权、TLS 网关、Origin 校验、限流和可信租户绑定。

## 报告漏洞

仓库已启用 GitHub Private vulnerability reporting。请通过仓库的 [私密漏洞报告入口](https://github.com/erduo1998-cell/reachsurge/security/advisories/new) 提交报告，不要使用公开 Issue、Discussion 或 Pull Request 披露未修复漏洞。

不要在任何公开位置粘贴 API Key、邮箱密码、代理凭证、客户数据或可直接利用的未修复细节。

报告时建议包含：

- 受影响版本或 commit；
- 最小复现步骤；
- 实际影响；
- 已遮罩的日志或截图；
- 建议修复方式（如有）。

如果怀疑凭证已经泄露，请先在对应服务商后台撤销并轮换凭证，再进行报告。不要等待代码修复后才轮换。

## 信任模型

- MCP 客户端、模型输出和外部网页内容都按不可信输入处理。
- `REACHSURGE_USER_ID` 是本地命名空间，不是身份认证。
- `.env` 是本机明文秘密文件，应限制权限、禁止提交 Git、禁止放入云同步公共目录。
- Fernet 保护数据库敏感字段，但不抵御本机账号或 Fernet Key 同时失陷。
- 真实发信是高风险外部动作，默认关闭；客户端仍应在调用时向人类确认。

## 凭证泄露应急

1. 立即撤销受影响的 API Key、邮箱应用密码或代理密码；
2. 生成新凭证并只写入本机 `.env`；
3. 检查 MCP 客户端日志、终端日志、Git 历史和云同步记录；
4. 清理包含旧凭证的本地运行数据或日志；
5. 重新运行项目测试和秘密扫描。
